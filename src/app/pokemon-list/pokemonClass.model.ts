
export class PokemonClass {
  // on met que les propriété dont on a besoin
  // pas besoin de mettre toute la structure de l'api si on utilise pas tout
  constructor(
    //public id?: string,
    public name?: string,
    //public nationalPokedexNumber?: number,
    public imageUrl?: string,
    //public imageUrlHiRes?: string
  ) {}
}
